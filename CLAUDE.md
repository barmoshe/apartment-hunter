# Project notes for AI agents

A public demo: a Hebrew/RTL apartment-comparison web app (Next.js 16, React 19,
TypeScript), with a clean, neutral product UI. All data lives in the browser's
`localStorage` — there is no server and no account.

See `AGENTS.md` for framework-version rules before writing any code.
